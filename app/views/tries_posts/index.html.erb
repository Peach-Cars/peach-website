<h1 class="text-3xl font-bold mb-6">Blog Posts</h1>

<% @posts_data['posts'].each do |post| %>
  <div class="blog-post bg-white shadow-md rounded-lg p-4 mb-4 transition-transform duration-200 hover:shadow-lg">
    <h2 class="text-2xl font-semibold mb-2"><%= post['title'] %></h2>
    <p class="text-gray-700 mb-4"><%= post['excerpt'] %></p>
    <a href="<%= post['url'] %>" class="text-blue-500 hover:text-blue-700">Read More</a>
  </div>
<% end %>
